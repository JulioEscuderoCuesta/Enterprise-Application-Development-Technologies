

public class User {

    private String name;
    private String password;
    private String iban;
    private UserType type;
    private Set<Series> startedSeries;
    private Set<Series> pendingSeries;
    private Set<Series> finishedSeries;
    private Set<Bill> billsPaid;
    private Set<Bill> pendingBills;

    /**
     * 
     */
    public User(String name, String password, String iban, UserType type) {
        this.name = user;
        this.password = password;
        this.iban =iban;
        this.type = type;
        startedSeries = new ArrayList<Series>();
        pendingSeries = new ArrayList<Series>();
        finishedSeries = new ArrayList<Series>();
        billsPaid = new ArrayList<Bill>();
        pendingBills = new ArrayList<Bill>();
    }

    //Getters & Setters
    public String getName() {
        return name;
    } 
    
    public void setName(String name) {
        this.name = name;
    }

    public String getPassword() {
        return password;
    }

    public void setPassword(String password) {
        this.password = password;
    }

    public String getIban() {
        return iban;
    }

    public void setIban(String iban) {
        this.iban = iban;
    }

    public User getUserType() {
        return type;
    }

    public void setUserType(String type) {
        this.type = type;
    }

    public Set<Bill> getBillsPaid() {
        return billsPaid;
    }

    public Set<Bill> getPendingBills() {
        return pendingBills;
    }
    
    /**************************************************************** */

    private Set<Bill> getBillCurrentMonth() {
        return pendingBills.get(pendingsBills.size() - 1);

    }

    private Set<Bill> getBillPerMonth(String month) {
        Bill billOfThatMonth;
        ArrayList<Bill> auxList = new ArrayList<Bill>();
        auxList.addAll(pendingBills);
        auxList.addAll(billsPaid);

        foreach(Bill bill in auxList) {
            if(bill.getReleaseDate().getMonth().equals(month))
                billOfThatMonth = bill;
        }
        return billOfthatMonth;
    }

    private void addBillToPendingBills(Bill bill) {
        pendingBills.add(bill);
    }

    private void addBillToBillsPaid(Bill bill) {
        billsPaid.add(bill);
    }

    public boolean addSeriesToPendingSeries(Series series) {
        pendingSeries.add(series);
    }

    public boolean addSeriesToStartedSeries(Series series) {
        startedSeries.add(series);
    }

    public boolean addSeriesToFinishedSeries(Series series) {
        addSeries.add(series);
    }


}